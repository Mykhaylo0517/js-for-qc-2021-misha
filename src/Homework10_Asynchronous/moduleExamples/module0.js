export const LOGIN = "superAdmin"; // constant export

export const users = ['Alex', 'Nick', 'Anna', 'Peter']; // array export

export function sum(a,b) { // function export
    return a + b;
}

export class Person {    // class export
  constructor(fullName) {
    this.fullName = fullName;
  }
}