function summ(x, y) {
    // let res = x + y;
    // res = parseFloat(res.toFixed(1)); 
    // return res;

    return parseFloat((x+y).toFixed(1)); // rounding and bringing to a number
}
module.exports = summ;